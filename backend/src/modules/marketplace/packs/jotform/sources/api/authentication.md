# Jotform MCP Authentication

OAuth authorization server: `https://oauth2.jotform.com`.

Protected resource: `https://mcp.jotform.com/mcp`.

Relay uses authorization code, S256 PKCE, refresh tokens, dynamic public-client registration, and provider revocation. Credentials are never agent inputs.
