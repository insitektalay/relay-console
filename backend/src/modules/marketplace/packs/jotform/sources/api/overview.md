# Jotform MCP Overview

Relay is the MCP client. Agents receive two governed wrappers, not the raw server.

Read tools: `form_list`, `get_submissions`.

Write tools: `create_form`, `edit_form`, `create_submission`.

The live schema discovered from Jotform is authoritative.
