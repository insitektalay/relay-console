# Jotform Approval Example

For a new form, present the exact `create_form` tool name and complete arguments. State that nothing has been created. After approval, execute those same arguments once, then verify the returned form ID with `form_list`.
