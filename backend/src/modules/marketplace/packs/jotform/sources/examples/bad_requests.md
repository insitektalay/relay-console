# Unsafe or Unsupported Jotform Requests

- Guess a Jotform REST endpoint when an MCP call fails.
- Change the form payload after approval.
- Retry a form creation without verifying whether the first call succeeded.
- Expose an OAuth token or legacy API key.
