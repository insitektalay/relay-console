# Jotform Write Workflows

Use only `create_form`, `edit_form`, or `create_submission`.

Before approval, show the exact target, intended fields, and material effects. Approval is bound to the final tool name and arguments. Do not change either after approval. After execution, verify the returned provider ID with a read before saying the write succeeded.
