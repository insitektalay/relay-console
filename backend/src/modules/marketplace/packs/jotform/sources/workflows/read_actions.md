# Jotform Read Workflows

Use `form_list` to resolve form IDs and confirm created forms. Use `get_submissions` only with arguments accepted by its live schema.

Return bounded results and distinguish an empty result from an authorization or provider failure.
