# Pipedrive API Authentication

{{CONNECTION_CONTEXT}}

{{CAPABILITY_CONTEXT}}

{{POLICY_CONTEXT}}

Use API token or OAuth bearer token authentication against the connected company domain. API tokens are user/company scoped and highly sensitive; OAuth access and refresh tokens are also secret.

Stop on invalid token, wrong company domain, revoked OAuth, insufficient permission, or role/visibility denial.
