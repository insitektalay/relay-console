# GitLab Approval-Required Examples

{{CONNECTION_CONTEXT}}

{{CAPABILITY_CONTEXT}}

{{POLICY_CONTEXT}}

- Protect the `main` branch for project `group/app` with the approved merge rules.
- Create a project webhook for merge request and pipeline events to the approved endpoint.
- Retry the failed production deployment job after the incident lead approves it.
- Update the masked CI/CD variable name `DEPLOY_TARGET` in the production project.
