# Intercom Safe Actions

{{CONNECTION_CONTEXT}}

{{CAPABILITY_CONTEXT}}

{{POLICY_CONTEXT}}

Safe mode requires matching approval for conversation count, bounded metadata list, and exact metadata reads. Dangerously skip permissions removes those Relay per-action prompts only; workspace/admin/region authority, provider permissions, fixed requests, limits, redaction, audit, revocation, and provider limits remain enforced.
