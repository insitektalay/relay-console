# Zendesk API Authentication

{{CONNECTION_CONTEXT}}

{{CAPABILITY_CONTEXT}}

{{POLICY_CONTEXT}}

Use OAuth bearer tokens or API token authentication against the configured Zendesk subdomain. Do not infer subdomains from user text when a connection already specifies one.

Authentication, authorization, or role errors must stop the workflow. Do not request raw API tokens in chat.
