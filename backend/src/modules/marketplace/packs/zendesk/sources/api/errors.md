# Zendesk Errors and Failure Modes

{{CONNECTION_CONTEXT}}

{{CAPABILITY_CONTEXT}}

{{POLICY_CONTEXT}}

Handle invalid subdomain, disabled token auth, expired OAuth, role/ticket-access denial, validation errors, invalid status transitions, public/internal comment mistakes, upload failures, attachment permission failures, ticket update conflicts, bulk job limits, rate limits, and provider outages.

On partial/bulk failure, stop and summarize safe error categories without retrying customer-visible or bulk writes blindly.
