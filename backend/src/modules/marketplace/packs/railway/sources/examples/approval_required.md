# Railway Approval-Required Examples

{{CONNECTION_CONTEXT}}

{{CAPABILITY_CONTEXT}}

{{POLICY_CONTEXT}}

- Redeploy service `api` in Railway environment `production` after the approved incident rollback note.
- Update the `DATABASE_URL` variable reference for the approved Railway service without displaying the value.
- Add the approved custom domain `api.example.com` to the selected Railway service.
- Create a Railway deployment webhook for the approved project and endpoint.
