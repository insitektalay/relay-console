# PostHog Approval-Required Examples

{{CONNECTION_CONTEXT}}

{{CAPABILITY_CONTEXT}}

{{POLICY_CONTEXT}}

- Increase PostHog feature flag `checkout-redesign` from 10% to the approved 25% rollout for the approved cohort.
- Export persons matching the approved filter `plan = enterprise` for the approved date window after privacy review.
- Create the approved PostHog CDP destination/webhook for `user_signed_up` events.
- Publicly share the approved dashboard only after confirming it contains no person-level or session replay data.
