# HubSpot Escalation

{{CONNECTION_CONTEXT}}

{{CAPABILITY_CONTEXT}}

{{POLICY_CONTEXT}}

Escalate when credentials are missing, scopes are insufficient, record ids or duplicate matches are ambiguous, a property/stage/owner is invalid, a request is customer-visible, bulk, export, delete/merge, webhook/workflow/property/admin related, or official docs do not cover the requested operation.

Do not proceed when the request asks to expose tokens, bypass HubSpot scopes, mass-message customers, impersonate a human, or delete the account/portal.
