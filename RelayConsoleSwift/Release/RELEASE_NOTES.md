# Relay Console release notes

Replace this file with reviewed, release-specific notes before creating a
protected `macos-v<version>-b<build>` tag. The release workflow embeds these
notes in Sparkle's signed appcast and refuses to publish without this file.
